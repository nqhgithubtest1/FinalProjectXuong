@model List<Book>

<div class="row">
    @foreach (var book in Model)
    {
        <div class="col-md-4 col-sm-6 mb-4">
            <div class="card h-100">
                <img class="card-img-top" src="@book.ImagePath" alt="@book.Title" style="height: 200px; object-fit: cover;">
                <div class="card-body">
                    <h5 class="card-title">@book.Title</h5>
                    <p class="card-text"><strong>Author:</strong> @book.Author</p>
                    <p class="card-text"><strong>Price:</strong> @book.Price.ToString("C")</p>
                    @Html.ActionLink("View Details", "Details", new { id = book.Id }, new { @class = "btn btn-primary" })
                </div>
            </div>
        </div>
    }
</div>
